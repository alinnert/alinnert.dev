<header>
  <nav>
    <ul>
      <li><a href="/" class="homepage-link">alinnert.dev</a></li>
      <li><a href="/blog">Blog</a></li>
      <li><a href="/projekte">Projekte</a></li>
    </ul>
  </nav>
</header>

<div class="gradient"></div>

<style lang="scss">
  @use "../stylesheets/mixins/text-gradient.scss" as *;
  
  header {
    display: flex;
    justify-content: center;
    position: fixed;
    top: var(--spacing-2);
    left: var(--spacing-2);
    right: var(--spacing-2);
    padding-block: var(--spacing-2);
    padding-inline: var(--spacing-2);
    border-radius: var(--theme-card--radius);
    background: var(--theme-glass--bg);
    backdrop-filter: blur(var(--theme-glass--blur));
    z-index: 10;
  }

  nav {
    grid-column: 3;
  }

  ul {
    display: flex;
    list-style-type: none;
  }

  a {
    color: var(--palette-neutral-text);
    font-size: var(--text-lg);
    font-weight: var(--font-weight-semibold);
    display: inline flow-root;
    padding-inline: var(--spacing-3);
    padding-block: var(--spacing-2);
  }
  
  .homepage-link {
    @include text-gradient(var(--gradient-logo));
    font-weight: var(--font-weight-extrabold);
  }

  .gradient {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 50px;
    z-index: 1;
    background: linear-gradient(
      to bottom,
      var(--theme-page--bg) var(--spacing-2),
      transparent
    );
  }
</style>
